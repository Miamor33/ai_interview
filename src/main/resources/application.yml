server:
  port: 8080

mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.czh.interview.entity
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

minio:
  url: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket: interview

# Spring AI配置
spring:
#  ai:
#    openai:
#      api-key: ${OPENAI_API_KEY:your-api-key-here}
#      chat:
#        options:
#          model: gpt-3.5-turbo
#          temperature: 0.7
  datasource:
    url: jdbc:mysql://localhost:3306/ai_interview?useSSL=false&serverTimezone=UTC
    username: root
    password: cczzhh
    driver-class-name: com.mysql.cj.jdbc.Driver
# 语音服务配置
speech:
  asr:
    url: http://localhost:8001/asr
  tts:
    url: http://localhost:8002/tts

# 实时反馈配置
feedback:
  polling:
    interval: 2000  # 轮询间隔（毫秒）
    enabled: true   # 是否启用轮询

# JWT配置
jwt:
  secret: interview-secret-key-change-in-production
  expiration: 86400000  # 24小时（毫秒）
